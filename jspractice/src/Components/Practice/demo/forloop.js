// for (statement-1; statement-2; statement-3) {
//     // code block
// }

// Statement-1 is executed only once at the time of initialization of the loop.
// Statement-2 defines the condition of the loop.
// Statement-3 is executed every time after the code block has been executed.

const arr = ['a', 'b', 'c', 'd', 'e', 'd']

for(let i=10; i>0; i = i+5) {
    if(i==6 || i==3) {
        continue;
    }
    console.log(i)
    if(i==3) {
        break
    }

}


























// const arr = ['a', 'b', 'c', 'd', 'e']

